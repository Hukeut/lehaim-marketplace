/**
 * Superseded by components/marketplace/Catalogue.tsx.
 *
 * L'ancienne tuile produit (ProductTile) posait un bouton d'ajout direct qui
 * écrivait dans un panier serveur (table `carts`) — un modèle que le backend
 * traiteur porté depuis lehaim-marketplace ne connaît pas. Le panier vit
 * maintenant dans le navigateur, et le composant qui le porte vit dans
 * Catalogue.tsx (client, quantités inline).
 */
export {};
