/**
 * Superseded — voir page.tsx dans ce même dossier : la fiche produit à part
 * a été retirée (pas de variante à configurer sur ce backend). Fichier gardé
 * vide plutôt que supprimé pour ne pas perturber les outils qui s'attendent
 * à le trouver.
 */
export {};
